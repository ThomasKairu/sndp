name = "sndp"
pages_build_output_dir = "dist"
compatibility_date = "2026-01-20"
compatibility_flags = ["nodejs_compat"]

# Non-sensitive configuration (safe to commit)
[vars]
PGHOST = "46.224.223.247"
PGPORT = "5432"
PGDATABASE = "provision_leads"
PGUSER = "provision_user"
N8N_WEBHOOK_CHAT = "https://n8n.provisionlands.co.ke/webhook/chatbot-lead"
N8N_CONTACT_WEBHOOK = "https://n8n.provisionlands.co.ke/webhook/contact-lead"
N8N_NEWSLETTER_WEBHOOK = "https://n8n.provisionlands.co.ke/webhook/newsletter"

# SECRETS (must be set via Cloudflare Dashboard → Workers & Pages → sndp → Settings → Variables and Secrets)
# These are NOT stored here – they are encrypted Cloudflare secrets:
#   PGPASSWORD           (set in Cloudflare Dashboard only — never commit the actual value)
#   N8N_INTERNAL_SECRET  (set in Cloudflare Dashboard only — never commit the actual value)
#   TURNSTILE_SECRET_KEY (set in Cloudflare Dashboard only — never commit the actual value)
