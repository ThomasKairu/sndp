# ============================================================================
# API Configuration (Cloudflare Pages Functions Gateway)
# ============================================================================
# SECURITY: React calls Cloudflare Pages Functions at /api/*
# Cloudflare Workers hold n8n webhook secrets safely and proxy requests
# 
# Flow: React → Cloudflare Worker (/api/*) → n8n → PostgreSQL/leads table
#
# For local development, leave empty (will use relative /api/* paths)
# For production, this is handled automatically by Cloudflare Pages
VITE_API_BASE=

# ============================================================================
# Cloudflare Turnstile (Bot Protection)
# ============================================================================
# Get your site key from: https://dash.cloudflare.com/?to=/:account/turnstile
# Leave empty to disable Turnstile (not recommended for production)
VITE_TURNSTILE_SITE_KEY=

# ============================================================================
# CLOUDFLARE SECRETS (set in Cloudflare Dashboard, NOT in .env)
# Dashboard → Workers & Pages → Your Project → Settings → Environment Variables
# ============================================================================
#
# n8n Webhook URLs:
#   N8N_CONTACT_WEBHOOK=https://n8n.provisionlands.co.ke/webhook/contact-lead
#   N8N_NEWSLETTER_WEBHOOK=https://n8n.provisionlands.co.ke/webhook/newsletter
#   N8N_CHATBOT_WEBHOOK=https://n8n.provisionlands.co.ke/webhook/chatbot-lead
#
# Internal Secret (prevents direct n8n access):
#   N8N_INTERNAL_SECRET=your-random-32-char-secret-here
#   Generate with: openssl rand -hex 32
#
# Turnstile Secret Key (for server-side verification):
#   TURNSTILE_SECRET_KEY=your-turnstile-secret-key
#
# ============================================================================
# n8n Configuration (in your n8n workflow):
# - Add IF node after Webhook trigger
# - Check: {{$json.headers["x-internal-secret"]}} equals N8N_INTERNAL_SECRET
# - If false, respond with 403 Forbidden
# ============================================================================
